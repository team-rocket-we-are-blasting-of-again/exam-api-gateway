server.port=${GATEWAY_SERVER_PORT:8082}

io.github.tobiasz.security.user-id-header-name=${GATEWAY_USER_ID_HEADER:user_id}
io.github.tobiasz.security.gateway-auth-method=${GATEWAY_AUTH_METHOD:basic}
io.github.tobiasz.security.internal-routes=/gateway

# R2DBC
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/postgres
spring.r2dbc.username=postgres
spring.r2dbc.password=postgres

# Flyway
spring.flyway.url=jdbc:postgresql://localhost:5432/postgres
spring.flyway.user=postgres
spring.flyway.password=postgres

# Actuator
#management.endpoint.gateway.enabled=true
#management.endpoint.metrics.enabled=true
#management.endpoint.httptrace.enabled=true
#management.endpoints.web.exposure.include=*